class LoginRepository {
  // final LoginProvider _loginProvider = LoginProvider();

  // Future<LoginResponse> login(LoginRequest loginRequest) async {
  //   return await _loginProvider.login(loginRequest);
  // }
}
